---

 - name: Upload site
   hosts: all
   tasks:
     - shell: 'ncftpput -R -v -u "danroot@edmachub.com" -p $(cat ftp_password) edmachub.com public_html ../build/*'

 - name: Upload api
   hosts: all
   tasks:
     - shell: 'ncftpput -R -v -u "danroot@edmachub.com" -p $(cat ftp_password) edmachub.com public_html/api ../api/*'
